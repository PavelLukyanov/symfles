<html>
<body>

{{ form_start(form) }}
{{ form_widget(form) }}

    <button type="submit">Отправить</button>

{{ form_end(form) }}


<table>
    <thead>
    <th>ID</th>
    <th>Name</th>
    </thead>
    <tbody>
    {% for post in posts %}
        <tr>
            <td>{{ post.id }}</td>
            <td>{{ post.name }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>

</body>
</html>